# locale.conf 把默认语言设成 zh_CN.UTF-8，必须在此列出才会被生成。构建期 sphinx（btrfs-progs 的
# man）按 LANG 调 setlocale('') 读到未生成的 locale 会抛 locale.Error，man 无法编出，出厂 ISO 的
# 运行时中文 locale 同样不可用。
# 不列 C.UTF-8:它是 glibc 内建 locale，无需也无法在纯 stage3 里经 locale-gen 编译，
# 列了会让 locale-gen 报 `not all compiled` 而中止。
en_US.UTF-8 UTF-8
zh_CN.UTF-8 UTF-8
zh_TW.UTF-8 UTF-8
