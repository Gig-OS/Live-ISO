# calamares 取自官方 ::gentoo 树的 3.4 系列。3.3 的稳定版只支持到 python3_13，而滚动树默认已是
# python3_14，REQUIRED_USE 无法满足；3.4.x 支持 python3_14。
# calamares-settings-gig 依赖无版本的 app-admin/calamares，不阻挡 3.4。
# 只屏蔽 3.5 及以上:calamares 大版本会改动模块与 settings schema，滚动树静默升级会使安装器失败。
# 放行整条 3.4.x，官方后续 3.4.z 无需手工跟进。
# 升到 3.5 前需在真机验证 calamares-settings-gig 的 settings 与 shellprocess 兼容，再改本文件与 verify-iso.sh。
>=app-admin/calamares-3.5
