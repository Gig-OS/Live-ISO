# 整套 ZFS 走 amd64 STABLE、且互相兼容,保证 zfs-kmod 稳编 + userland↔module 版本对齐。
# 全局 ACCEPT_KEYWORDS="~amd64 *" 默认挑最新测试版,踩过两个实机坑:
#   1) 内核测试版 7.1.3 超 OpenZFS 上限 → zfs.ko 编不进、被 --keep-going 静默跳过(装不了 ZFS)。
#   2) userland 测试版 zfs-2.4.3 → 拖来 RC 模块 zfs-kmod-2.4.0_rc2,且跟 userland 对不齐版本
#      (userland/kmod 必须同版本,Gentoo 用 ~sys-fs/zfs-kmod-${PV} 锁)→ 出锅 ZFS 照样不能用。
# 做法(全 stable、彼此兼容):
#   内核: 最新 amd64-stable dist-kernel = 6.18.37_p1(mask 掉 6.18.38 起的测试版点发布 + 7.x)。
#   ZFS:  最新 amd64-stable = 2.3.6(userland + kmod 同为 2.3.6,mask 掉 2.3.7 起的测试版)。
#         zfs-kmod-2.3.6 的 MODULES_KERNEL_MAX=6.19 ≥ 内核 6.18 → 编得进;两边同 2.3.6 → 版本对齐。
# 维护: 出了更新的 amd64-stable 内核/ZFS(或要追新特性)后把版本号往上抬,注意保持
#        内核 ≤ zfs-kmod 的 MODULES_KERNEL_MAX、且 zfs 与 zfs-kmod 同版本。
# 出锅前 99-sanitize 硬验 zfs.ko 在位【且】userland/kmod 版本一致,双保险。
>=sys-kernel/gentoo-kernel-bin-6.18.38
>=sys-kernel/gentoo-kernel-6.18.38
>=sys-kernel/gentoo-sources-6.18.38
>=sys-fs/zfs-2.3.7
>=sys-fs/zfs-kmod-2.3.7
